name: Deprecated - use Supabase backend deploy

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is deprecated to avoid double-deploys."
          echo "Use: .github/workflows/supabase-backend-deploy.yml"
