/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Hvis du trenger turbopack root i en nyere Next-versjon, legg til riktig felt her
};
module.exports = nextConfig;
